spring:
  application:
    name: consumer-service-0
  cloud:
    loadbalancer:
      retry:
        enabled: true
    config:
      name: weixin,users,comsumer,weixinurl
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
      fail-fast: true
server:
  port: 5001
eureka:
  client:
    service-url:
      defaultZone: http://192.168.2.1:8761/eureka/
management:
  scurity:
    enabled: false
  server:
    port: 5002
#  endpoint:
#    health:
#      enabled: true
#    refresh:
#      enabled: true
#    heapdump:
#      enabled: true
#  server:
#    port: 9931
  health:
    mail:
      enabled: true